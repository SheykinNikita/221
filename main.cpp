#include <iostream>
#include <tgmath.h>

int bar (int a, int b);
double bar (double m, double t, double p);
RES = u/(sqrt(2)*(3,14)*pow(o,2)*p*N*6,022*pow(10,23));
return RES;
int main() {
    std::cout << bar << std::endl;
    return 0;
}